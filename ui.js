import { getGenres, getMovies, getMovieInfo } from "./api.js";
import {
  createMoviePoster,
  createMovieTitle,
  createMovieOverview,
  getRandomMovie,
} from "./helpers.js";

export const showRandomMovie = async () => {
  // Implementation
};

export const populateGenreDropdown = (genres) => {
  // Implementation
};

// Other UI related functions
