export const createMoviePoster = (posterPath) => {
  // Implementation
};

export const createMovieTitle = (title) => {
  // Implementation
};

export const createMovieOverview = (overview) => {
  // Implementation
};

export const getRandomMovie = (movies) => {
  // Implementation
};
